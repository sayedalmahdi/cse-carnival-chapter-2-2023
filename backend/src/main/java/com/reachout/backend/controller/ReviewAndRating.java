package com.reachout.backend.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/doctor-ratings")
public class ReviewAndRating {
//    @Autowired
//    private ReviewsAndRatingsRepository reviewsAndRatingsRepository;

//    @GetMapping("/{doctorId}")
//    public ResponseEntity<Integer> getDoctorRating(@PathVariable Long doctorId) {
////        // Calculate the average rating for the specified doctor
////        Double averageRating = reviewsAndRatingsRepository.calculateAverageRatingForDoctor(doctorId);
////
////        if (averageRating != null) {
////            return ResponseEntity.ok(averageRating);
////        } else {
////            return ResponseEntity.notFound().build();
////        }
//
//}

}



